# Solo Instrument Exercise Coach (MVP Scaffold)

## Run
- Install deps: `npm install`
- Dev server: `npm run dev`

## Notes
- Generator is deterministic with seed.
- Audio uses Web Audio (count-in + simple synth).
- Saved exercises and logs are stored in IndexedDB via Dexie.
